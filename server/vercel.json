{
  "version": 2,
  "builds": [
    {
      "src": "server/src/*.ts",
      "use": "@vercel/node"
    },
    {
      "src": "server/src/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/src/$1"
    }
  ]
}
