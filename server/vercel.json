{
  "version": 2,
  "builds": [
    {
      "src": "src/*.ts",
      "use": "@vercel/node"
    },
    {
      "src": "src/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/$1"
    }
  ]
}
